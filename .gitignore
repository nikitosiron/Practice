# Зависимости — ставятся через `npm install`, в репозиторий не нужны
node_modules/

# Логи и временные файлы
*.log
npm-debug.log*

# Системный мусор (.DS_Store — macOS, Thumbs.db — Windows)
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Переменные окружения (на будущее — туда кладут секреты)
.env

NOTES.md

# Локальные конфиги для запуска (VS Code launch и подобные)
.claude/